
export enum AmountTypeEnum {
  percentage = 1,
  static,
  cashback
}

export enum PromoCodeStatusEnum {
  pending = 1,
  active,
  stopped,
  expired,
}

export enum PromoCodeUsageTypeEnum {
  single = 1,
  group
}

export enum PromoCodeTypeEnum {
  generateCode = 1,
  customCode
}

export enum UserPromoCodeBonusTypeEnum {
  firstLogin = 1
}

export enum BonusTypeEnum {
  firstLogin = 1
}